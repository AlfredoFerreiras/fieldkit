# Base URL of the Fieldkit sync server, e.g. https://api.example.com
# Leave unset to run against the in-memory mock server in src/sync/api.ts,
# which exercises the full pipeline including conflicts, offline queueing,
# and backoff without a backend.
EXPO_PUBLIC_API_URL=
